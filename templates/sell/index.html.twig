{% extends './base.html.twig' %}
{% block title %} Sells {% endblock %}
{% block body %}
    <div class="card mt-3" style="">
        <div class="card-body">
            <h5 class="card-title">Sell List</h5>
            <p class="card-text">
            <table class="table">
                <thead>
                <tr>
                    <th scope="col">#</th>
                    <th scope="col">Quantity</th>
                    <th scope="col">Price</th>
                    <th scope="col">Created Time</th>
                </tr>
                </thead>
                <tbody>
                {% for sell in sells %}
                    <tr>
                        <th scope="row">{{ sell.getId() }}</th>
                        <td>{{ sell.getQuantity() }}</td>
                        <td>{{ sell.getPrice() }}</td>
                        <td>{{ sell.getCreatedAt().format('Y-m-d H:i:s') }}</td>
                    </tr>
                {% endfor %}
                </tbody>
            </table>
            </p>
        </div>
    </div>

{% endblock %}